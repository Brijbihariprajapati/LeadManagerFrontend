/** Dispatched after team user list should refresh (create / modal). */
export const LMS_ADMIN_USERS_CHANGED = 'lms-admin-users-changed';

/** Dispatched after a user creates a lead from the header modal — refresh my-leads tables + dashboard summary. */
export const LMS_USER_LEADS_REFRESH = 'lms-user-leads-refresh';
